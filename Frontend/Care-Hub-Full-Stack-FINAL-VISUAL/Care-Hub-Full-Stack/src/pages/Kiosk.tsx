import {useState} from "react"; import {MonitorSmartphone,ArrowRight,Check} from "lucide-react"; import Button from "../components/Button";
export default function Kiosk(){const [step,setStep]=useState(0);const steps=[["Welcome","Choose a language to begin."],["Select patient","Choose the person for this session."],["Today's Meds","Review the four core actions."],["Session complete","The session is saved and the kiosk can reset."]];return <div className="kiosk-screen"><div className="kiosk-top"><b><MonitorSmartphone/> Care Hub Kiosk</b><button onClick={()=>setStep(0)}>Reset</button></div><div className="kiosk-card"><div className="kiosk-icon">{step===0?"👋":step===1?"👤":step===2?"💊":"✓"}</div><span className="eyebrow">KIOSK MODE</span><h1>{steps[step][0]}</h1><p>{steps[step][1]}</p>{step===0?<div className="kiosk-buttons"><Button onClick={()=>setStep(1)}>English <ArrowRight/></Button><Button variant="secondary" onClick={()=>setStep(1)}>हिन्दी <ArrowRight/></Button></div>:step===1?<div className="kiosk-buttons"><Button onClick={()=>setStep(2)}>Meena Sharma <ArrowRight/></Button></div>:step===2?<div className="kiosk-buttons"><Button onClick={()=>setStep(3)}>Start / Complete Session <Check/></Button></div>:<div className="kiosk-buttons"><Button onClick={()=>setStep(0)}>Finish & Reset</Button></div>}</div></div>}