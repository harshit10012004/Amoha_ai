import {HeartHandshake,UserRound,Stethoscope} from "lucide-react"; import {useTranslation} from "react-i18next"; import {useNavigate} from "react-router-dom"; import type {Role} from "../types";
export default function Role(){const {t}=useTranslation(),nav=useNavigate();const pick=(r:Role)=>{localStorage.setItem("carehub-role",r);nav("/onboarding/profile")};return <div className="flow-page"><FlowTitle icon={<HeartHandshake/>} title={t("role")}/><div className="role-grid"><RoleButton icon={<HeartHandshake/>} text={t("caregiver")} click={()=>pick("caregiver")}/><RoleButton icon={<UserRound/>} text={t("recipient")} click={()=>pick("recipient")}/><RoleButton icon={<Stethoscope/>} text={t("healthWorker")} click={()=>pick("healthWorker")}/></div></div>}
function RoleButton({icon,text,click}:{icon:any;text:string;click:()=>void}){return <button className="role-card" onClick={click}>{icon}<b>{text}</b></button>}
function FlowTitle({icon,title}:{icon:any;title:string}){return <div className="flow-title"><span>{icon}</span><h1>{title}</h1></div>}