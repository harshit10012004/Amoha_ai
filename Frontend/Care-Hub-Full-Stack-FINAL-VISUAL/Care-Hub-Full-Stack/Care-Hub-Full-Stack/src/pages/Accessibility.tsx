import { Accessibility, ArrowLeft, Type, Contrast, Languages, Volume2 } from "lucide-react"; import {useNavigate} from "react-router-dom"; import {useState} from "react";
export default function AccessibilityPage(){const nav=useNavigate(),[large,setLarge]=useState(false),[high,setHigh]=useState(false),[voice,setVoice]=useState(true);return <div className={large?'large-ui':''}><button className="back-link" onClick={()=>nav('/home')}><ArrowLeft/> Back</button><div className="page-title"><h1><Accessibility className="title-icon"/> Accessibility</h1><p>Adjust the experience for comfort and clarity.</p></div><div className={`settings-list ${high?'high-contrast':''}`}><Setting icon={<Type/>} title="Large text" desc="Make important text easier to read." on={large} toggle={()=>setLarge(!large)}/><Setting icon={<Contrast/>} title="High contrast" desc="Increase visual contrast." on={high} toggle={()=>setHigh(!high)}/><Setting icon={<Volume2/>} title="Voice support" desc="Keep voice-first controls available." on={voice} toggle={()=>setVoice(!voice)}/><div className="setting-row"><Languages/><div><b>Language</b><p>Use the English / हिन्दी switch in the header.</p></div></div></div></div>}
function Setting({icon,title,desc,on,toggle}:{icon:any;title:string;desc:string;on:boolean;toggle:()=>void}){return <div className="setting-row">{icon}<div><b>{title}</b><p>{desc}</p></div><button className={`toggle ${on?'on':''}`} onClick={toggle}><i/></button></div>}
