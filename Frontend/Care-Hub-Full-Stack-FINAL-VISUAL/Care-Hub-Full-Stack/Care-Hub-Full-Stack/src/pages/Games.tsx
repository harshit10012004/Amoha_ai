import { Brain, ArrowLeft, ChevronRight } from "lucide-react";
import { useNavigate } from "react-router-dom";
export default function Games(){ const nav=useNavigate(); return <div><button className="back-link" onClick={()=>nav('/home')}><ArrowLeft/> Back</button><div className="page-title"><h1><Brain className="title-icon"/> Games</h1><p>Choose one activity. Take your time.</p></div><div className="game-list"><button className="game-list-card" onClick={()=>nav('/games/memory')}><span className="game-emoji">🧠</span><div><b>Memory Matching</b><p>Match pairs with large, easy-to-see cards.</p></div><ChevronRight/></button><button className="game-list-card" onClick={()=>nav('/games/memory')}><span className="game-emoji">🔢</span><div><b>Simple Sequence</b><p>Follow a short visual sequence.</p></div><ChevronRight/></button></div></div> }
