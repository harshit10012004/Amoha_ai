import {HeartPulse,ArrowRight} from "lucide-react"; import Button from "../components/Button"; import {useTranslation} from "react-i18next"; import {useNavigate} from "react-router-dom";
export default function Welcome(){const {t}=useTranslation(),nav=useNavigate();return <div className="center-page"><div className="hero-card"><div className="hero-logo"><HeartPulse/></div><div className="eyebrow">CARE HUB</div><h1>{t("welcome")}</h1><p>{t("tagline")}</p><Button onClick={()=>nav("/onboarding/language")}>{t("getStarted")} <ArrowRight/></Button><button className="text-button" onClick={()=>nav("/login")}>{t("login")}</button></div></div>}