import {ArrowLeft,BookOpen,MapPin,HeartHandshake,PlayCircle,ChevronRight} from 'lucide-react';
import {useNavigate} from 'react-router-dom';
export default function Resources(){const nav=useNavigate();const items=[['Daily care tips','Simple ideas for routines, activities and communication.',<BookOpen/>],['Activities & engagement','Music, conversation, drawing and familiar daily activities.',<PlayCircle/>],['Local support','Find community programmes and caregiver support nearby.',<MapPin/>],['Caregiver support','Practical guidance and a place to ask for help.',<HeartHandshake/>]];return <div><button className="back-link" onClick={()=>nav('/home')}><ArrowLeft/> Back to Home</button><div className="page-title"><span className="eyebrow">LEARN & CONNECT</span><h1>Care Resources</h1><p>Helpful information without overwhelming the person you care for.</p></div><div className="resource-grid">{items.map(([t,d,icon])=><button className="resource-card" key={t as string}>{<span>{icon}</span>}<div><b>{t as string}</b><p>{d as string}</p></div><ChevronRight/></button>)}</div><div className="resource-note"><b>Personalized later</b><p>When location and role are connected, Care Hub can surface relevant local programmes, trusted resources and caregiver guidance.</p></div></div>}
