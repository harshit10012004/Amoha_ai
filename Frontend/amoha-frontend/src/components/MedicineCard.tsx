import {Check,XCircle} from "lucide-react"; import type {Medicine} from "../types"; import Button from "./Button"; import {useTranslation} from "react-i18next";
export default function MedicineCard({m,onTake,onSkip}:{m:Medicine;onTake:()=>void;onSkip:()=>void}){const {t}=useTranslation();return <article className={`medicine-card ${m.status==="taken"?"done":""}`}><div><small>{m.time}</small><h3>{m.name}</h3><p>{m.dosage}</p></div>{m.status==="taken"?<span className="taken"><Check/> {t("taken")}</span>:<div className="med-actions"><Button onClick={onTake}><Check/> {t("take")}</Button><button className="skip" onClick={onSkip}><XCircle/> {t("skip")}</button></div>}</article>}