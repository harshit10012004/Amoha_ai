import {ArrowLeft,Bell,CheckCircle2,Info,AlertTriangle} from 'lucide-react';
import {useNavigate} from 'react-router-dom';
export default function Notifications(){const nav=useNavigate();const items=[['Medicine reminder','Morning medicine is ready.','Now',<Bell/>],['Care plan','3 of 5 daily tasks completed.','Today',<CheckCircle2/>],['Sync','Your latest offline activity is ready to sync.','Today',<Info/>],['Caregiver','A care team member viewed today’s status.','Yesterday',<AlertTriangle/>]];return <div><button className="back-link" onClick={()=>nav('/home')}><ArrowLeft/> Back to Home</button><div className="page-title"><h1>Notifications</h1><p>Only the things worth your attention.</p></div><div className="notification-list">{items.map(([t,d,time,icon])=><div className="notification" key={t as string}><span>{icon}</span><div><b>{t as string}</b><p>{d as string}</p></div><small>{time as string}</small></div>)}</div></div>}
