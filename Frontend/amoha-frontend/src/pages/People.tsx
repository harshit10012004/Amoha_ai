import { ArrowLeft, ShieldCheck, UserRound } from "lucide-react"; import {useState} from "react"; import {useNavigate} from "react-router-dom";
export default function People(){const nav=useNavigate(),[ok,setOk]=useState(false);return <div><button className="back-link" onClick={()=>nav('/home')}><ArrowLeft/> Back</button><div className="page-title"><h1>People & Memories</h1><p>Face/name recall is optional and permission-based.</p></div><div className="memory-card privacy-card"><div className="memory-avatar"><UserRound/></div><h2>Family & People</h2><p>Enable this only if the care recipient and family agree. The real face/name service can connect through the backend later.</p><div className="consent-row"><ShieldCheck/><span>Opt-in permission</span><button className={`toggle ${ok?'on':''}`} onClick={()=>setOk(!ok)}><i/></button></div>{ok&&<div className="success-banner">Permission enabled for this demo.</div>}</div></div>}
