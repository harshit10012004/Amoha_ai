import {UserRound} from "lucide-react"; import Button from "../components/Button"; import {useTranslation} from "react-i18next"; import {useNavigate} from "react-router-dom";
export default function Profile(){const {t}=useTranslation(),nav=useNavigate();return <div className="flow-page"><div className="flow-title"><span><UserRound/></span><h1>{t("profile")}</h1></div><label className="field">{t("name")}<input id="recipientName" placeholder="e.g. Meena Sharma"/></label><Button onClick={()=>{const n=(document.getElementById("recipientName") as HTMLInputElement).value||"Meena Sharma";localStorage.setItem("carehub-name",n);localStorage.setItem("carehub-onboarded","1");nav("/home")}}>{t("continue")}</Button></div>}