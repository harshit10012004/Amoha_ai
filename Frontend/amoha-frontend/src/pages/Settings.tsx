import {ArrowLeft,Accessibility,Languages,Bell,Shield,LogOut,ChevronRight} from 'lucide-react';
import {useNavigate} from 'react-router-dom';
export default function Settings(){const nav=useNavigate();const rows=[['Accessibility','/accessibility',<Accessibility/>],['Language','/onboarding/language',<Languages/>],['Notifications','/notifications',<Bell/>],['Privacy & permissions','/people',<Shield/>]];return <div><button className="back-link" onClick={()=>nav('/home')}><ArrowLeft/> Back to Home</button><div className="page-title"><h1>Settings</h1><p>Make Care Hub comfortable for you.</p></div><div className="settings-menu">{rows.map(([t,p,i])=><button key={t as string} onClick={()=>nav(p as string)}><span>{i}</span><b>{t as string}</b><ChevronRight/></button>)}</div><button className="logout-button" onClick={()=>{localStorage.removeItem("carehub-token"); localStorage.removeItem("carehub-onboarded"); localStorage.removeItem("carehub-name");nav('/welcome')}}><LogOut/> Sign out of this device</button></div>}
