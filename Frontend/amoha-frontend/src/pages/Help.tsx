import {useState} from "react"; import {useTranslation} from "react-i18next"; import HelpCard from "../components/HelpCard"; import {ArrowLeft,MessageCircleHeart} from "lucide-react";
const topics=[["tips","Dementia Care Tips","🌿","Keep routines calm, simple and predictable."],["agitation","Handling Agitation","🤝","Give space, speak slowly and reduce stimulation."],["sleep","Sleep Problems","🌙","Keep a consistent evening routine."],["medicine","Medicine Help","💊","Follow the prescribed schedule and record changes."]];
export default function Help(){const {t}=useTranslation(),[topic,setTopic]=useState<string|null>(null);if(topic){const x=topics.find(a=>a[0]===topic)!;return <><button className="back-link" onClick={()=>setTopic(null)}><ArrowLeft/> {t("back")}</button><div className="help-detail"><span>{x[2]}</span><h1>{x[1]}</h1><p>{x[3]}</p><button className="whatsapp" onClick={()=>window.open(`https://wa.me/?text=${encodeURIComponent(x[1]+": "+x[3])}`,"_blank")}><MessageCircleHeart/> {t("share")}</button></div></>}return <><div className="page-title"><h1>{t("help")}</h1><p>{t("helpTopics")}</p></div><div className="stack">{topics.map(x=><HelpCard key={x[0]} title={x[1]} emoji={x[2]} onClick={()=>setTopic(x[0])}/>)}</div></>}